{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.przepis_delete'|trans({'%id%': przepis.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="container">
    <h1>{{ 'Usuń użytkownika'|trans({'%id%': przepis.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('user_delete', {id: user.id}) }) }}
    {{ form_widget(form) }}

        <input type="submit" value="Usuń uzytkownika" class="btn btn-primary" />


    {{ form_end(form) }}
    </div>
{% endblock %}