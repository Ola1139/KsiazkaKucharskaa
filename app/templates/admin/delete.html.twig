{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.przepis_delete'|trans({'%id%': przepis.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="container">

    <h1>{{ 'Usuń przepis'|trans({'%id%': przepis.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('przepis_admin_delete', {id: przepis.id}) }) }}
    {{ form_widget(form) }}

        <input type="submit" value="Usuń" class="btn btn-primary" />
    </div>


    {{ form_end(form) }}
    </div>
{% endblock %}