{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.przepis_edit'|trans({'%id%': przepis.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.przepis_edit'|trans({'%id%': przepis.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('przepis_edit', {id: przepis.id}) }) }}
    {{ form_widget(form) }}

    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('stronaglowna_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}